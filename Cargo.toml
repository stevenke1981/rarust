[workspace]
resolver = "3"
members = [
    "rarust-core",
    "rarust",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
authors = ["Rarust Contributors"]

[profile.release]
lto = true
strip = true
codegen-units = 1
